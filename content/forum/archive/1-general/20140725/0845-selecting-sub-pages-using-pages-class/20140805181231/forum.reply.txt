User: fitzage

----

Date: 2014-08-05 18:12:31

----

Text: If I understand the issue correctly, you can do one of two things:

1. Use the full path with `find()`.
2. Use `findByUID()` if you want to use just the slug.